apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kauppalista-client-ingressroute
  namespace: kauppalista
spec:
  routes:
    - match: Host(`kauppalista.apps.k3s.kelhala.com`)
      kind: Rule
      services:
        - name: kauppalista-client-svc
          port: 80